#!/bin/sh

cd /opt/app/src
./build.sh clean install -Pcommunity -DskipTests -Didlj-enabled=true -am -pl rts/lra/lra-coordinator
cp rts/lra/lra-coordinator/target/lra-coordinator-swarm.jar /opt/app/openshift-app.jar

exit 0
