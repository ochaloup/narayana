<arquillian>

<!--    <extension qualifier="webdriver">
        <property name="browser">phantomjs</property>
    </extension>-->

    <container qualifier="container-1" default="true">
        <configuration>
            <property name="host">localhost</property>
            <property name="port">${swarm.arquillian.daemon.port:12345}</property>
        </configuration>
    </container>

    <container qualifier="container-2">
        <configuration>
            <property name="host">localhost</property>
            <property name="port">${swarm.arquillian.daemon.port:12346}</property>
        </configuration>
    </container>
</arquillian>

